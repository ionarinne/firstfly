// 从用户读取输入值
scanf_s("%d %d", &a, &b);

// 从输入值中分离出小时和分钟
int c = a / 100; // 从 a 中提取小时部分
int d = b / 60; // 从 b 中提取小时部分
int e = a % 100; // 从 a 中提取分钟部分
int f = b % 60; // 从 b 中提取分钟部分

// 计算总时间
int g = (60 * c + e + 60 * d + f) / 60; // 将小时和分钟转换为总分钟数，再计算小时数
int l = (60 * c + e + 60 * d + f) % 60; // 将小时和分钟转换为总分钟数，再计算分钟数

// 输出结果
printf("%d", 100 * g + l);

return 0;
