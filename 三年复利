#include <stdio.h>

int main() {
    // 声明变量 x
    int x;

    // 从用户输入中读取一个整数，将其存储在 x 中
    scanf_s("%d", &x);

    // 计算复利公式
    double amount = x * (1 + 0.033) * (1 + 0.033) * (1 + 0.033);

    // 将计算结果打印到标准输出
    printf("%f", amount);

    // 程序运行成功，返回 0 表示程序正常结束
    return 0;
}
